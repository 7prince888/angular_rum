MONGODB_URI=mongodb://localhost:27017
DB_NAME=test
COLLECTION=items
PORT=3000

# Optional: create a text index on fields you want to search with ?q=term
# Example (run in mongo shell):
# db.items.createIndex({ name: 'text', description: 'text' })
